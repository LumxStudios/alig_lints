name: example_flutter
description: expect_lint goldens for the Flutter rules.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  alig_lints:
    path: ../
  custom_lint: ^0.8.1
