name: example_single_widget
description: >
  expect_lint goldens for prefer-single-widget-per-file, which needs a package
  whose other files each hold one widget — see doc/LIMITATIONS.md.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  alig_lints:
    path: ../
  custom_lint: ^0.8.1
