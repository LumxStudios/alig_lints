include: ../analysis_options.yaml

analyzer:
  errors:
    # Tests must construct CustomLintConfigs / LintOptions, both of which are
    # marked @internal by custom_lint_core. Production code only ever reads a
    # configs object handed to it, so this relaxation is scoped to tests.
    invalid_use_of_internal_member: ignore
