include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    todo: ignore
  exclude:
    - example/**
    - example_flutter/**

linter:
  rules:
    - always_declare_return_types
    - prefer_final_locals
    - unawaited_futures
